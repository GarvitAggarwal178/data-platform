findata:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5433
      dbname: findata
      user: admin
      password: secret
      schema: public
      threads: 4